spring.application.name=ms-user
server.port=${SERVER_PORT:8082}

# Default profile uses MySQL below. For local IDE runs without MySQL, activate profile "dev"
# (loads application-dev.properties with in-memory H2). See .run/MsUserApplication.run.xml

# Eureka Configuration
eureka.instance.prefer-ip-address=true
eureka.instance.instance-id=${spring.cloud.client.hostname}:${spring.application.name}:${server.port}
eureka.client.service-url.defaultZone=${DISCOVERY_URL:http://localhost:8761/eureka/}
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3307/user_service1?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

logging.level.com.example.service.DataGeneratorService=DEBUG
logging.level.com.github.javafaker=ERROR

key.secret= 28e65c6afd4f3e61b9689a52ba0d8eb2528a0121686378028494a76f90900687

# Mail Configuration
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:marwenbelakhel333@gmail.com}
spring.mail.password=${MAIL_PASSWORD:nasx ctwu ldln sbcx}
spring.mail.from=${MAIL_FROM:noreply@smarthire.tn}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Password Reset Configuration
app.reset-password.expiration-hours=1
app.reset-password.code-length=6

# Face Recognition Python service endpoint
face.recognition.service.url=http://localhost:5050


