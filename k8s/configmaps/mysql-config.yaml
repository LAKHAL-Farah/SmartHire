apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-config
  namespace: smarthire
data:
  my.cnf: |
    [mysqld]
    character-set-server=utf8mb4
    collation-server=utf8mb4_unicode_ci
    max_connections=500
