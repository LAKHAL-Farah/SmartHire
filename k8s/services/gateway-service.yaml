apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: smarthire
spec:
  selector:
    app: gateway
  ports:
    - protocol: TCP
      port: 8887
      targetPort: 8887
  type: ClusterIP
